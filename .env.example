# Copy this to .env and fill in your values before starting.

# Required. Used to derive the encryption key for secrets at rest.
# Use something long and random - treat it like a root password.
MASTER_PASSWORD=

# Optional. Defaults to 3000.
PORT=3000
